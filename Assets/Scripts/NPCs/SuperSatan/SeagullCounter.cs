using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SeagullCounter : MonoBehaviour
{
    public static float Count;
    // Start is called before the first frame update
    void Start()
    {
        Count += 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
